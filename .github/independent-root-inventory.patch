diff --git a/.github/workflows/independent-parameter-domains.yml b/.github/workflows/independent-parameter-domains.yml
index bef8994d..c2206d8a 100644
--- a/.github/workflows/independent-parameter-domains.yml
+++ b/.github/workflows/independent-parameter-domains.yml
@@ -47,11 +47,14 @@ jobs:
           git rev-parse HEAD | tee independent-parameter-evidence/head.txt
           test "$(git rev-parse HEAD)" = '${{ github.event.pull_request.head.sha || github.sha }}'
           git diff --check
+          python3 morphhdl/scripts/check-native-source-preservation.py 2>&1 | tee independent-parameter-evidence/source-preservation-live.log
           python3 morphhdl/scripts/check-native-source-preservation.py --self-test 2>&1 | tee independent-parameter-evidence/source-preservation.log
+          python3 morphhdl/scripts/check-production-retirement.py 2>&1 | tee independent-parameter-evidence/retirement-live.log
           python3 morphhdl/scripts/check-production-retirement.py --self-test 2>&1 | tee independent-parameter-evidence/retirement.log
+          python3 morphhdl/scripts/check-typed-layering-ir.py 2>&1 | tee independent-parameter-evidence/typed-layering-live.log
           python3 morphhdl/scripts/check-typed-layering-ir.py --self-test 2>&1 | tee independent-parameter-evidence/typed-layering.log
           cd repro/independent-parameters
-          python3 -m unittest -v test_check test_qualify
+          python3 -m unittest -v test_check test_qualify test_layering
       - uses: actions/upload-artifact@v4
         if: always()
         with:
diff --git a/morphhdl/contracts/increment-54-typed-layering-ir.contract b/morphhdl/contracts/increment-54-typed-layering-ir.contract
index dcc7f10a..73374036 100644
--- a/morphhdl/contracts/increment-54-typed-layering-ir.contract
+++ b/morphhdl/contracts/increment-54-typed-layering-ir.contract
@@ -14,6 +14,7 @@
     "morphplugin/src/main",
     "morphruntime/src/main",
     "paramrtl/src/main",
+    "repro/independent-parameters/src/main",
     "scalaplugin/src/main",
     "sim/src/main",
     "tester/src/main"
@@ -391,6 +392,7 @@
         "morphplugin/src/main/",
         "morphruntime/src/main/",
         "paramrtl/src/main/",
+        "repro/independent-parameters/src/main/",
         "scalaplugin/src/main/",
         "sim/src/main/",
         "tester/src/main/"
diff --git a/morphhdl/scripts/check-typed-layering-ir.py b/morphhdl/scripts/check-typed-layering-ir.py
index b92f48c9..f396f120 100644
--- a/morphhdl/scripts/check-typed-layering-ir.py
+++ b/morphhdl/scripts/check-typed-layering-ir.py
@@ -18,7 +18,7 @@ from typing import Any, Dict, Iterable, List, Mapping, Optional, Sequence, Tuple
 
 DEFAULT_MANIFEST = "morphhdl/contracts/increment-54-typed-layering-ir.contract"
 EXPECTED_REPOSITORY = "pysolvesemi/MorphHDL"
-EXPECTED_CONTRACT_SHA256 = "44e1df89aaafccc843c9fb696441940b20232f95d751b1fb9c973f6df8edb09c"
+EXPECTED_CONTRACT_SHA256 = "0bede059b85d46778134d389947e092e5f1b9953458825a33aaa9fd743fcaa47"
 EXPECTED_PLUGIN_COMPONENTS = (
     "MorphHdlTypedElaborationControlComponent",
     "MorphHdlNaturalSymbolicConditionalComponent",
diff --git a/repro/independent-parameters/test_layering.py b/repro/independent-parameters/test_layering.py
new file mode 100644
index 00000000..8d328f3d
--- /dev/null
+++ b/repro/independent-parameters/test_layering.py
@@ -0,0 +1,55 @@
+"""Ensure the standalone application is scanned, never an audit exception."""
+import importlib.util
+import json
+import tempfile
+import unittest
+from pathlib import Path
+
+ROOT = Path(__file__).resolve().parents[2]
+REPRO_ROOT = 'repro/independent-parameters/src/main'
+SPEC = importlib.util.spec_from_file_location(
+    'independent_layering_guard', ROOT / 'morphhdl/scripts/check-typed-layering-ir.py')
+GUARD = importlib.util.module_from_spec(SPEC)
+SPEC.loader.exec_module(GUARD)
+
+
+class StandaloneLayeringTests(unittest.TestCase):
+    def setUp(self):
+        self.manifest = GUARD.load_manifest(ROOT / GUARD.DEFAULT_MANIFEST)
+        self.contract = GUARD.compile_contract(self.manifest)
+        self.temporary = tempfile.TemporaryDirectory(prefix='independent-layering-')
+        self.addCleanup(self.temporary.cleanup)
+        self.root = Path(self.temporary.name)
+        for source_root in self.contract['roots']:
+            (self.root / source_root).mkdir(parents=True)
+        GUARD.write_build_fixtures(self.root, self.contract['build'])
+        GUARD.write_required_files(self.root, self.contract)
+        self.probe = self.root / REPRO_ROOT / 'scala/repro/AuditProbe.scala'
+        self.probe.parent.mkdir(parents=True)
+        self.probe.write_text('package repro\nobject AuditProbe\n')
+        GUARD.validate_sources(self.root, self.contract)
+
+    def test_reproducer_is_a_scanned_high_level_application(self):
+        self.assertIn(REPRO_ROOT, self.contract['roots'])
+        self.assertNotIn(REPRO_ROOT, self.manifest['low_level_source_roots'])
+        self.assertGreater(GUARD.validate_sources(ROOT, self.contract), 0)
+
+    def test_retired_sidecar_in_reproducer_is_rejected(self):
+        self.probe.write_text('package repro\nobject ExternalParameterizedMemoryRegistry\n')
+        with self.assertRaisesRegex(GUARD.LayeringError, 'obsolete-parameterized-sidecar-symbol'):
+            GUARD.validate_sources(self.root, self.contract)
+
+    def test_duplicate_typed_owner_in_reproducer_is_rejected(self):
+        self.probe.write_text('package repro\nfinal class ElabInt\n')
+        with self.assertRaisesRegex(GUARD.LayeringError, 'elab-int-owner'):
+            GUARD.validate_sources(self.root, self.contract)
+
+    def test_narrowing_reproducer_out_of_inventory_is_rejected(self):
+        narrowed = json.loads(json.dumps(self.manifest))
+        narrowed['production_source_roots'].remove(REPRO_ROOT)
+        with self.assertRaisesRegex(GUARD.LayeringError, 'closed Increment 54 contract'):
+            GUARD.compile_contract(narrowed)
+
+
+if __name__ == '__main__':
+    unittest.main()
