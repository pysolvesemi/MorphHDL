name: MorphHDL Increment 53c proof reconciliation cleanup

on:
  push:
    branches:
      - agent/increment-53c-reconcile-20260828
  workflow_dispatch:

concurrency:
  group: morphhdl-increment-53c-proof-reconciliation
  cancel-in-progress: true

jobs:
  cleanup:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Confirm reconciliation is superseded by the merged Increment 53c
        run: echo "Increment 53c is merged; no reconciliation polling remains."
