name: Increment 59i direct local-enable publisher

on:
  push:
    branches: ['agent/increment-59i-combined-reduction-closure']
    paths:
      - '.github/workflows/increment-59i-local-enable-direct-publish.yml'
      - 'morphhdl/repair-59i-composite-local-enable/**'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: increment-59i-direct-local-enable-publisher
  cancel-in-progress: false

jobs:
  publish:
    name: Exact local-enable candidate, both Scala lanes
    runs-on: ubuntu-latest
    timeout-minutes: 240
    container:
      image: ghcr.io/spinalhdl/docker:v1.2.0
    env:
      BRANCH: agent/increment-59i-combined-reduction-closure
      COURSIER_REPOSITORIES: https://repo.maven.apache.org/maven2
      COURSIER_CACHE: /sbt/.cache/coursier/v1
      JAVA_TOOL_OPTIONS: '-Xmx5G -XX:ActiveProcessorCount=4'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          submodules: recursive
          persist-credentials: true

      - name: Prepare one exact committed candidate
        shell: bash
        run: |
          set -euo pipefail
          out="$GITHUB_WORKSPACE/target/increment-59i-direct-local-enable"
          mkdir -p "$out"
          expected=$(git rev-parse HEAD)
          test "$(git ls-remote origin "refs/heads/$BRANCH" | awk '{print $1}')" = "$expected"
          test -z "$(git status --porcelain)"
          printf '%s\n' "$expected" > "$out/expected-head.txt"

          if grep -q 'controlValues: Vector\[BaseType\]' \
              morphhdl/src/main/scala/spinal/core/internals/TypedBalancedReductionBridgeReplay.scala && \
             grep -q 'callback.assignments ++ callback.statements' \
              morphhdl/src/main/scala/spinal/core/internals/TypedBalancedReductionCompositeReplay.scala && \
             test -f morphhdl/src/test/scala/spinal/core/internals/TypedBalancedReductionCompositeLocalEnableTests.scala; then
            printf 'already-promoted\n' | tee "$out/mode.txt"
          else
            printf 'apply-and-promote\n' | tee "$out/mode.txt"
            python3 morphhdl/repair-59i-composite-local-enable/promote-reviewed-ci.py | \
              tee "$out/review-generation.log"
            git diff --check
            git status --porcelain=v1 > "$out/generated-status.txt"
            test -s "$out/generated-status.txt"
            python3 morphhdl/repair-59i-composite-local-enable/check-generated-paths.py | \
              tee "$out/generated-inventory.log"

            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git add -- \
              docs/morphhdl/increment-59i-composite-local-enables.md \
              morphhdl/contracts/increment-59i-local-enable-review.json \
              morphhdl/scripts/check-increment-59i-local-enable-source-review.py \
              morphhdl/scripts/check-increment-59i-source-review.py \
              morphhdl/scripts/test-increment-59i-local-enable-source-review.py \
              morphhdl/src/main/scala/spinal/core/internals/TypedBalancedReductionBridgeReplay.scala \
              morphhdl/src/main/scala/spinal/core/internals/TypedBalancedReductionCompositeReplay.scala \
              morphhdl/src/test/scala/spinal/core/internals/TypedBalancedReductionCompositeLocalEnableTests.scala
            git diff --cached --check
            test -z "$(git status --porcelain=v1 | grep '^??' || true)"
            git commit -m '59i: promote reviewed composite local enables'
          fi

          git rev-parse HEAD > "$out/candidate-head.txt"
          git rev-parse 'HEAD^{tree}' > "$out/candidate-tree.txt"
          test -z "$(git status --porcelain)"

      - name: Require exact generated successor review
        shell: bash
        run: |
          set -euo pipefail
          out="$GITHUB_WORKSPACE/target/increment-59i-direct-local-enable"
          grep -q 'controlValues: Vector\[BaseType\]' \
            morphhdl/src/main/scala/spinal/core/internals/TypedBalancedReductionBridgeReplay.scala
          grep -q 'BRIDGE-CONTROL-REGISTER' \
            morphhdl/src/main/scala/spinal/core/internals/TypedBalancedReductionCompositeReplay.scala
          grep -q 'callback.assignments ++ callback.statements' \
            morphhdl/src/main/scala/spinal/core/internals/TypedBalancedReductionCompositeReplay.scala
          grep -q 'external and registered peer controls remain rejected' \
            morphhdl/src/test/scala/spinal/core/internals/TypedBalancedReductionCompositeLocalEnableTests.scala
          ! grep -q 'BalancedLocalEnableOps' \
            morphhdl/src/test/scala/spinal/core/internals/TypedBalancedReductionCompositeLocalEnableTests.scala
          test -f morphhdl/contracts/increment-59i-local-enable-review.json
          test -f morphhdl/scripts/check-increment-59i-local-enable-source-review.py
          test -f morphhdl/scripts/test-increment-59i-local-enable-source-review.py
          {
            python3 morphhdl/scripts/check-increment-59i-local-enable-source-review.py --self-test
            python3 morphhdl/scripts/test-increment-59i-local-enable-source-review.py
            python3 morphhdl/scripts/check-native-source-preservation.py
            python3 morphhdl/scripts/check-typed-native-source-overlay.py
          } 2>&1 | tee "$out/source-review.log"
          test -z "$(git status --porcelain)"

      - name: Bootstrap pinned SBT
        shell: bash
        run: |
          set -euo pipefail
          out="$GITHUB_WORKSPACE/target/increment-59i-direct-local-enable"
          version=$(sed -n 's/^sbt.version=//p' project/build.properties)
          test "$version" = '1.10.0'
          base="https://repo.maven.apache.org/maven2/org/scala-sbt/sbt-launch/$version/sbt-launch-$version.jar"
          curl --fail --location --retry 5 --retry-all-errors "$base" -o "$out/sbt-launch.jar"
          printf '[repositories]\nlocal\nmaven-central: https://repo.maven.apache.org/maven2\n' > "$out/repositories"
          cat > "$out/sbt.sh" <<'SH'
          set -euo pipefail
          out="$GITHUB_WORKSPACE/target/increment-59i-direct-local-enable"
          scala="$1"; shift
          java -Xmx4G -Dsbt.log.noformat=true -Dsbt.supershell=false \
            -Dsbt.boot.directory=/sbt/.sbt/boot -Dsbt.global.base=/sbt/.sbt \
            -Dsbt.ivy.home=/sbt/.ivy2 -Duser.home=/sbt \
            -Dsbt.repository.config="$out/repositories" -Dsbt.override.build.repos=true \
            -jar "$out/sbt-launch.jar" "++$scala" "$@"
          SH
          chmod +x "$out/sbt.sh"

      - name: Focused local-enable diagnostics and Scala 2.12.18 regressions
        shell: bash
        run: |
          set -euo pipefail
          out="$GITHUB_WORKSPACE/target/increment-59i-direct-local-enable"
          focused="$out/local-enable-2.12.18.log"
          status=0
          "$out/sbt.sh" 2.12.18 'morph/Test/compile' \
            'morph/Test/testOnly spinal.core.internals.TypedBalancedReductionCompositeLocalEnableTests' \
            > "$focused" 2>&1 || status=$?
          cat "$focused"
          test "$status" -eq 0
          grep -q 'All tests passed' "$focused"
          ! grep -q '\*\*\* FAILED \*\*\*' "$focused"

          regression="$out/regression-2.12.18.log"
          status=0
          "$out/sbt.sh" 2.12.18 \
            'morph/Test/testOnly spinal.core.internals.TypedBalancedReductionCompositeWideningTests spinal.core.internals.TypedBalancedReductionCompositeTests spinal.core.internals.TypedBalancedReductionBridgeReplayTests spinal.core.internals.TypedBalancedReductionCompositeCallbackPolicyTests spinal.core.internals.TypedBalancedReductionCompositeCaptureTests spinal.core.internals.TypedBalancedReductionCombinedTests' \
            > "$regression" 2>&1 || status=$?
          tail -n 500 "$regression"
          test "$status" -eq 0
          grep -q 'All tests passed' "$regression"
          ! grep -q '\*\*\* FAILED \*\*\*' "$regression"

      - name: Focused local-enable diagnostics and Scala 2.13.12 regressions
        shell: bash
        run: |
          set -euo pipefail
          out="$GITHUB_WORKSPACE/target/increment-59i-direct-local-enable"
          focused="$out/local-enable-2.13.12.log"
          status=0
          "$out/sbt.sh" 2.13.12 'morph/Test/compile' \
            'morph/Test/testOnly spinal.core.internals.TypedBalancedReductionCompositeLocalEnableTests' \
            > "$focused" 2>&1 || status=$?
          cat "$focused"
          test "$status" -eq 0
          grep -q 'All tests passed' "$focused"
          ! grep -q '\*\*\* FAILED \*\*\*' "$focused"

          regression="$out/regression-2.13.12.log"
          status=0
          "$out/sbt.sh" 2.13.12 \
            'morph/Test/testOnly spinal.core.internals.TypedBalancedReductionCompositeWideningTests spinal.core.internals.TypedBalancedReductionCompositeTests spinal.core.internals.TypedBalancedReductionBridgeReplayTests spinal.core.internals.TypedBalancedReductionCompositeCallbackPolicyTests spinal.core.internals.TypedBalancedReductionCompositeCaptureTests spinal.core.internals.TypedBalancedReductionCombinedTests' \
            > "$regression" 2>&1 || status=$?
          tail -n 500 "$regression"
          test "$status" -eq 0
          grep -q 'All tests passed' "$regression"
          ! grep -q '\*\*\* FAILED \*\*\*' "$regression"

      - name: Push only from the unchanged expected branch head
        shell: bash
        run: |
          set -euo pipefail
          out="$GITHUB_WORKSPACE/target/increment-59i-direct-local-enable"
          expected=$(cat "$out/expected-head.txt")
          candidate=$(cat "$out/candidate-head.txt")
          remote=$(git ls-remote origin "refs/heads/$BRANCH" | awk '{print $1}')
          test "$remote" = "$expected"
          test "$(git rev-parse HEAD)" = "$candidate"
          test -z "$(git status --porcelain)"
          if [ "$candidate" != "$expected" ]; then
            git push origin "HEAD:refs/heads/$BRANCH"
          fi
          test "$(git ls-remote origin "refs/heads/$BRANCH" | awk '{print $1}')" = "$candidate"
          printf '%s\n' "$candidate" > "$out/published-head.txt"

      - name: Upload exact direct-publisher evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: increment-59i-direct-local-enable-${{ github.sha }}
          path: target/increment-59i-direct-local-enable
          if-no-files-found: error
          retention-days: 14
