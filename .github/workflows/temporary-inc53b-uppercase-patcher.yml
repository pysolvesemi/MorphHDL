name: Temporary Increment 53b uppercase formal repair

on:
  push:
    branches:
      - fix/increment-53b-uppercase-formal-equivalence

permissions:
  contents: write

concurrency:
  group: temporary-inc53b-uppercase-patcher-${{ github.ref }}
  cancel-in-progress: false

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[inc53b-uppercase-patched]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repair branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply reviewed uppercase naming and formal-equivalence repair
        shell: bash
        run: |
          set -euo pipefail
          python3 <<'PY'
          from pathlib import Path

          localizer_path = Path("morphhdl/src/main/scala/spinal/core/internals/MorphHdlExternalEnumLocalizer.scala")
          localizer = localizer_path.read_text(encoding="utf-8")
          old_comment = '''  * Every enum value is declared as a short module-local `localparam` using the
  * element name. Global preprocessor macros and enum/module prefixes therefore
  * do not escape the module that uses the enum. A same-module short-name
  * collision fails closed rather than silently reintroducing a prefix.
'''
          new_comment = '''  * Every enum value is declared as an uppercase, enum-qualified module-local
  * `localparam`: for example, Scala `State.IDLE` becomes Verilog `STATE_IDLE`.
  * Component, module and hierarchy names are never prefixed. Global
  * preprocessor macros therefore do not escape the module that uses the enum,
  * while distinct enum types remain readable inside one module.
'''
          if old_comment not in localizer:
              raise SystemExit("enum-localizer documentation anchor is missing")
          localizer = localizer.replace(old_comment, new_comment, 1)

          old_element = '''        val localName = requiredName(
          element.getName(),
          s"element ${element.position} of enum '$enumName'"
        )
        validateLocalName(component, enumName, localName)
'''
          new_element = '''        val elementName = requiredName(
          element.getName(),
          s"element ${element.position} of enum '$enumName'"
        )
        validateLocalName(component, enumName, elementName)
'''
          if old_element not in localizer:
              raise SystemExit("enum element-name anchor is missing")
          localizer = localizer.replace(old_element, new_element, 1)

          old_native = '''        val nativeName = enumName + encodingSuffix + "_" + localName
        val value = encoding.getValue(element)
'''
          new_native = '''        val nativeName = enumName + encodingSuffix + "_" + elementName
        val localName = nativeName.toUpperCase(java.util.Locale.ROOT)
        validateLocalName(component, enumName, localName)
        val value = encoding.getValue(element)
'''
          if old_native not in localizer:
              raise SystemExit("enum native-name anchor is missing")
          localizer = localizer.replace(old_native, new_native, 1)
          localizer = localizer.replace(
              "requires short localparam '${alias.localName}'",
              "requires uppercase enum localparam '${alias.localName}'"
          )
          localizer = localizer.replace(
              "a prefix-free enum localparam would be ambiguous",
              "an uppercase enum-qualified localparam would be ambiguous"
          )
          localizer_path.write_text(localizer, encoding="utf-8")

          test_path = Path("morphhdl/src/test/scala/morphhdl/SpinalEnumLocalParameterTests.scala")
          test_path.write_text(r'''package morphhdl

import java.nio.charset.StandardCharsets
import java.nio.file.{Files, Path}

import scala.collection.JavaConverters._
import scala.sys.process.{Process, ProcessLogger}

import org.scalatest.funsuite.AnyFunSuite
import spinal.core._

import morphhdl.frontend.HdlInt

object Inc53bGlobalBinaryState extends SpinalEnum(binarySequential) {
  val IDLE, RUN = newElement()
  setGlobal()
}

object Inc53bGlobalOneHotState extends SpinalEnum(binaryOneHot) {
  val IDLE, RUN = newElement()
  setGlobal()
}

object Inc53bGlobalCollisionState extends SpinalEnum(binarySequential) {
  val IDLE, WAIT, RUN = newElement()
  setGlobal()
}

object Inc53bFormalState extends SpinalEnum(binarySequential) {
  val IDLE, LOAD, RUN, DONE = newElement()
  setGlobal()
}

final class Inc53bBinaryEnumLeaf extends Component {
  setDefinitionName("Inc53bBinaryEnumLeaf")

  val select = in Bool()
  val active = out Bool()

  val state = Inc53bGlobalBinaryState()
  state := Inc53bGlobalBinaryState.IDLE
  when(select) {
    state := Inc53bGlobalBinaryState.RUN
  }
  active := state === Inc53bGlobalBinaryState.RUN
}

final class Inc53bOneHotEnumLeaf extends Component {
  setDefinitionName("Inc53bOneHotEnumLeaf")

  val select = in Bool()
  val active = out Bool()

  val state = Inc53bGlobalOneHotState()
  state := Inc53bGlobalOneHotState.IDLE
  when(select) {
    state := Inc53bGlobalOneHotState.RUN
  }
  active := state === Inc53bGlobalOneHotState.RUN
}

final class Inc53bEnumTop(width: HdlInt) extends Component {
  setDefinitionName("Inc53bEnumTop")

  val payload = in(morphhdl.frontend.UInt(width bits))
  val binaryActive = out Bool()
  val oneHotActive = out Bool()

  val binary = new Inc53bBinaryEnumLeaf
  val oneHot = new Inc53bOneHotEnumLeaf
  binary.select := payload(0)
  oneHot.select := payload(0)
  binaryActive := binary.active
  oneHotActive := oneHot.active
}

final class Inc53bEnumCollisionTop(width: HdlInt) extends Component {
  setDefinitionName("Inc53bEnumCollisionTop")

  val payload = in(morphhdl.frontend.UInt(width bits))
  val active = out Bool()

  val left = Inc53bGlobalBinaryState()
  val right = Inc53bGlobalCollisionState()
  left := Inc53bGlobalBinaryState.IDLE
  right := Inc53bGlobalCollisionState.IDLE
  when(payload(0)) {
    left := Inc53bGlobalBinaryState.RUN
    right := Inc53bGlobalCollisionState.RUN
  }
  active :=
    (left === Inc53bGlobalBinaryState.RUN) ^
      (right === Inc53bGlobalCollisionState.RUN)
}

final class Inc53bFormalEnumTop(width: HdlInt) extends Component {
  setDefinitionName("Inc53bFormalEnumTop")

  val selector = in(morphhdl.frontend.UInt(width bits))
  val enable = in Bool()
  val active = out Bool()
  val encoded = out Bits (2 bits)

  val state = Reg(Inc53bFormalState()) init (Inc53bFormalState.IDLE)
  switch(state) {
    is(Inc53bFormalState.IDLE) {
      when(enable) {
        state := Inc53bFormalState.LOAD
      }
    }
    is(Inc53bFormalState.LOAD) {
      when(!enable) {
        state := Inc53bFormalState.IDLE
      } otherwise {
        when(selector(0)) {
          state := Inc53bFormalState.RUN
        }
      }
    }
    is(Inc53bFormalState.RUN) {
      when(enable) {
        state := Inc53bFormalState.DONE
      }
    }
    is(Inc53bFormalState.DONE) {
      when(!enable) {
        state := Inc53bFormalState.IDLE
      }
    }
  }

  active := state === Inc53bFormalState.DONE
  encoded := state.asBits
}

class SpinalEnumLocalParameterTests extends AnyFunSuite {
  test("MorphVerilog replaces global enum macros with uppercase enum-qualified module-local parameters") {
    withTemporaryDirectory { directory =>
      val firstDirectory = directory.resolve("first")
      val replayDirectory = directory.resolve("replay")
      Files.createDirectories(firstDirectory)
      Files.createDirectories(replayDirectory)

      val first = generate(firstDirectory)
      val replay = generate(replayDirectory)
      assert(
        java.util.Arrays.equals(
          Files.readAllBytes(first),
          Files.readAllBytes(replay)
        ),
        "module-local enum publication was not byte deterministic"
      )

      val verilog = read(first)
      val binary = module(verilog, "Inc53bBinaryEnumLeaf")
      val oneHot = module(verilog, "Inc53bOneHotEnumLeaf")

      assert(!verilog.contains("`define"))
      assert(!verilog.contains("`Inc53bGlobal"))
      assert(!verilog.contains("localparam Inc53bGlobal"))
      assert(!verilog.contains("localparam IDLE ="))
      assert(!verilog.contains("localparam RUN ="))

      assert(binary.contains("localparam INC53BGLOBALBINARYSTATE_IDLE = 1'd0;"))
      assert(binary.contains("localparam INC53BGLOBALBINARYSTATE_RUN = 1'd1;"))
      assert(!binary.contains("INC53BBINARYENUMLEAF_INC53BGLOBALBINARYSTATE"))

      assert(oneHot.contains("localparam INC53BGLOBALONEHOTSTATE_IDLE = 2'd1;"))
      assert(oneHot.contains("localparam INC53BGLOBALONEHOTSTATE_IDLE_OH_ID = 0;"))
      assert(oneHot.contains("localparam INC53BGLOBALONEHOTSTATE_RUN = 2'd2;"))
      assert(oneHot.contains("localparam INC53BGLOBALONEHOTSTATE_RUN_OH_ID = 1;"))
      assert(!oneHot.contains("INC53BONEHOTENUMLEAF_INC53BGLOBALONEHOTSTATE"))

      assert(
        verilog.split("localparam INC53BGLOBALBINARYSTATE_IDLE =", -1).length - 1 == 1
      )
      assert(
        verilog.split("localparam INC53BGLOBALONEHOTSTATE_IDLE =", -1).length - 1 == 1
      )
      lint(first, directory, "Inc53bEnumTop")
    }
  }

  test("ordinary SpinalVerilog keeps native global enum macro behavior") {
    withTemporaryDirectory { directory =>
      val config = SpinalConfig(targetDirectory = directory.toString)
      config.netlistFileName = "native_enum.v"
      SpinalVerilog(config) {
        component()
      }

      val verilog = read(directory.resolve("native_enum.v"))
      assert(verilog.contains("`define Inc53bGlobalBinaryState_IDLE"))
      assert(verilog.contains("`define Inc53bGlobalOneHotState_IDLE"))
      assert(verilog.contains("`Inc53bGlobalBinaryState_RUN"))
      assert(verilog.contains("`Inc53bGlobalOneHotState_RUN"))
      assert(!verilog.contains("localparam INC53BGLOBALBINARYSTATE_IDLE"))
    }
  }

  test("enum-qualified uppercase names avoid same-module element collisions") {
    withTemporaryDirectory { directory =>
      val config = SpinalConfig(targetDirectory = directory.toString)
      config.netlistFileName = "enum_collision.v"
      val report = MorphVerilog(config) {
        val width = HdlInt.param("WIDTH", default = 4, min = 1, max = 16)
        new Inc53bEnumCollisionTop(width)
      }

      assert(report.toplevelName == "Inc53bEnumCollisionTop")
      val output = directory.resolve("enum_collision.v")
      val verilog = read(output)
      assert(verilog.contains("localparam INC53BGLOBALBINARYSTATE_IDLE = 1'd0;"))
      assert(verilog.contains("localparam INC53BGLOBALCOLLISIONSTATE_IDLE = 2'd0;"))
      assert(verilog.contains("localparam INC53BGLOBALCOLLISIONSTATE_WAIT = 2'd1;"))
      assert(!verilog.contains("localparam IDLE ="))
      lint(output, directory, "Inc53bEnumCollisionTop")
    }
  }

  test("Yosys formally proves legacy macro and uppercase localparam enum RTL equivalent") {
    withTemporaryDirectory { directory =>
      assert(commandAvailable("yosys"), "Yosys is required for the enum formal-equivalence contract")

      val legacyDirectory = directory.resolve("legacy")
      val localizedDirectory = directory.resolve("localized")
      Files.createDirectories(legacyDirectory)
      Files.createDirectories(localizedDirectory)

      val legacyConfig = formalConfig(legacyDirectory)
      legacyConfig.netlistFileName = "enum_macro.v"
      SpinalVerilog(legacyConfig) {
        formalComponent()
      }
      val legacyRtl = legacyDirectory.resolve("enum_macro.v")

      val localizedConfig = formalConfig(localizedDirectory)
      localizedConfig.netlistFileName = "enum_localparam.v"
      val report = MorphVerilog(localizedConfig) {
        formalComponent()
      }
      val localizedRtl = localizedDirectory.resolve("enum_localparam.v")

      assert(report.parameters.map(_.name) == Vector("WIDTH"))
      val legacy = read(legacyRtl)
      val localized = read(localizedRtl)
      assert(legacy.contains("`define Inc53bFormalState_IDLE"))
      assert(legacy.contains("`Inc53bFormalState_DONE"))
      assert(localized.contains("localparam INC53BFORMALSTATE_IDLE = 2'd0;"))
      assert(localized.contains("localparam INC53BFORMALSTATE_DONE = 2'd3;"))
      assert(!localized.contains("`define Inc53bFormalState"))
      assert(!localized.contains("`Inc53bFormalState"))

      val script = directory.resolve("enum_macro_vs_localparam.ys")
      val scriptText =
        s"""read_verilog -formal ${legacyRtl.toAbsolutePath}
           |prep -top Inc53bFormalEnumTop
           |rename Inc53bFormalEnumTop gold
           |design -stash gold
           |
           |read_verilog -formal ${localizedRtl.toAbsolutePath}
           |chparam -set WIDTH 4 Inc53bFormalEnumTop
           |prep -top Inc53bFormalEnumTop
           |rename Inc53bFormalEnumTop gate
           |design -stash gate
           |
           |design -reset
           |design -copy-from gold -as gold gold
           |design -copy-from gate -as gate gate
           |equiv_make gold gate equiv
           |hierarchy -check -top equiv
           |proc
           |opt_clean
           |equiv_simple -seq 8
           |equiv_induct -undef -seq 8
           |equiv_status -assert
           |""".stripMargin
      Files.write(script, scriptText.getBytes(StandardCharsets.UTF_8))

      val proof = run(directory, Seq("yosys", "-s", script.toString))
      assert(
        proof._1 == 0,
        s"Yosys failed macro/localparam formal equivalence:\n${proof._2}"
      )
    }
  }

  private def component(): Inc53bEnumTop = {
    val width = HdlInt.param("WIDTH", default = 4, min = 1, max = 16)
    new Inc53bEnumTop(width)
  }

  private def formalComponent(): Inc53bFormalEnumTop = {
    val width = HdlInt.param("WIDTH", default = 4, min = 1, max = 16)
    new Inc53bFormalEnumTop(width)
  }

  private def formalConfig(directory: Path): SpinalConfig =
    SpinalConfig(
      targetDirectory = directory.toString,
      defaultConfigForClockDomains = ClockDomainConfig(
        resetKind = SYNC,
        resetActiveLevel = HIGH
      )
    )

  private def generate(directory: Path): Path = {
    val config = SpinalConfig(targetDirectory = directory.toString)
    config.netlistFileName = "enum_localparams.v"
    val report = MorphVerilog(config) {
      component()
    }
    assert(report.toplevelName == "Inc53bEnumTop")
    assert(report.parameters.map(_.name) == Vector("WIDTH"))
    val output = directory.resolve("enum_localparams.v")
    assert(report.generatedSourcesPaths == Vector(output.toString))
    output
  }

  private def module(verilog: String, name: String): String =
    ("(?ms)^\\s*module\\s+" + java.util.regex.Pattern.quote(name) +
      "\\b.*?^\\s*endmodule\\b").r
      .findFirstIn(verilog)
      .getOrElse(fail(s"Module '$name' is missing"))

  private def lint(rtl: Path, directory: Path, top: String): Unit = {
    if (commandAvailable("iverilog")) {
      val result = run(
        directory,
        Seq(
          "iverilog",
          "-g2001",
          "-s",
          top,
          "-o",
          directory.resolve(top + ".out").toString,
          rtl.toString
        )
      )
      assert(result._1 == 0, s"iverilog rejected localized enum RTL:\n${result._2}")
    }

    if (commandAvailable("verilator")) {
      val result = run(
        directory,
        Seq(
          "verilator",
          "--lint-only",
          "--language",
          "1364-2001",
          "-Wno-fatal",
          "-Wno-DECLFILENAME",
          "--top-module",
          top,
          rtl.toString
        )
      )
      assert(result._1 == 0, s"Verilator rejected localized enum RTL:\n${result._2}")
    }

    if (commandAvailable("yosys")) {
      val result = run(
        directory,
        Seq(
          "yosys",
          "-q",
          "-p",
          s"read_verilog ${rtl.toString}; hierarchy -check -top $top; proc; check"
        )
      )
      assert(result._1 == 0, s"Yosys rejected localized enum RTL:\n${result._2}")
    }
  }

  private def commandAvailable(command: String): Boolean =
    Process(Seq("sh", "-c", s"command -v $command >/dev/null 2>&1")).! == 0

  private def run(directory: Path, command: Seq[String]): (Int, String) = {
    val output = new StringBuilder
    val logger = ProcessLogger(
      line => output.append(line).append('\n'),
      line => output.append(line).append('\n')
    )
    Process(command, directory.toFile).!(logger) -> output.result()
  }

  private def read(path: Path): String =
    new String(Files.readAllBytes(path), StandardCharsets.UTF_8)

  private def withTemporaryDirectory(body: Path => Unit): Unit = {
    val directory = Files.createTempDirectory("morphhdl-enum-localparam-test-")
    try body(directory)
    finally deleteTree(directory)
  }

  private def deleteTree(root: Path): Unit =
    if (Files.exists(root)) {
      val stream = Files.walk(root)
      try {
        stream.iterator().asScala.toVector.sortBy(_.getNameCount).reverse.foreach { path =>
          Files.deleteIfExists(path)
        }
      } finally stream.close()
    }
}
''', encoding="utf-8")

          workflow_path = Path(".github/workflows/morphhdl-enum-localparams.yml")
          workflow_path.write_text('''name: MorphHDL uppercase module-local SpinalEnum parameters and formal equivalence

# Increment 53b localizes native SpinalEnum macros in MorphHDL-owned publication
# code, emits uppercase enum-qualified module-local parameters, and formally
# proves equivalence to the untouched native macro implementation.
on:
  push:
    branches:
      - main
      - parameterized-verilog
      - fix/increment-53b-uppercase-formal-equivalence
  pull_request:
    branches:
      - main
      - parameterized-verilog
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: morphhdl-enum-localparams-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  source-boundary:
    name: MorphHDL-only uppercase enum source boundary
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out complete history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Reject native SpinalHDL implementation changes
        shell: bash
        run: |
          set -euo pipefail
          python3 morphhdl/scripts/check-native-source-preservation.py
          test -f morphhdl/src/main/scala/spinal/core/internals/MorphHdlExternalEnumLocalizer.scala
          grep -Fq 'nativeName.toUpperCase(java.util.Locale.ROOT)' \
            morphhdl/src/main/scala/spinal/core/internals/MorphHdlExternalEnumLocalizer.scala
          grep -Fq 'MorphHdlExternalEnumLocalizer.rewrite(pc)' \
            morphhdl/src/main/scala/morphhdl/MorphVerilog.scala
          grep -Fq 'equiv_induct -undef -seq 8' \
            morphhdl/src/test/scala/morphhdl/SpinalEnumLocalParameterTests.scala
          grep -Fq 'equiv_status -assert' \
            morphhdl/src/test/scala/morphhdl/SpinalEnumLocalParameterTests.scala
          if [[ -n "${GITHUB_BASE_REF:-}" ]]; then
            git fetch --no-tags origin "${GITHUB_BASE_REF}:refs/remotes/origin/${GITHUB_BASE_REF}"
            changed="$(git diff --name-only "origin/${GITHUB_BASE_REF}...HEAD")"
            if grep -Eq '^(core|lib|idslplugin)/src/main/' <<<"${changed}"; then
              echo "Increment 53b follow-up must not modify native SpinalHDL production sources" >&2
              grep -E '^(core|lib|idslplugin)/src/main/' <<<"${changed}" >&2
              exit 1
            fi
          fi

  contracts:
    name: Uppercase enum formal-equivalence proof Scala ${{ matrix.scala_version }}
    needs: source-boundary
    runs-on: ubuntu-latest
    timeout-minutes: 75
    container:
      image: ghcr.io/spinalhdl/docker:latest
    env:
      XDG_CACHE_HOME: /tmp/morphhdl-cache
    strategy:
      fail-fast: false
      matrix:
        scala_version:
          - "2.12.18"
          - "2.13.12"
    steps:
      - name: Check out sources
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify formal tool availability
        shell: bash
        run: |
          set -euo pipefail
          command -v yosys
          yosys -V

      - name: Install pinned Mill bootstrap
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --location --retry 5 --retry-all-errors \
            https://repo.maven.apache.org/maven2/com/lihaoyi/mill-dist/1.1.0/mill-dist-1.1.0.exe \
            --output /tmp/morphhdl-mill
          chmod +x /tmp/morphhdl-mill

      - name: Validate uppercase enum localization, formal equivalence and inherited publication
        shell: bash
        run: |
          set -euo pipefail
          /tmp/morphhdl-mill morph[${{ matrix.scala_version }}].testOnly \
            morphhdl.SpinalEnumLocalParameterTests \
            morphhdl.MorphSingleSourceVerilogTests \
            morphhdl.integration.ExternalSpinalVerilogBaselineTests
''', encoding="utf-8")

          todo_path = Path("docs/morphhdl/parameterized-verilog-todo.md")
          todo = todo_path.read_text(encoding="utf-8")
          old_todo = '''- [x] **Increment 53b — MorphHDL-owned module-local SpinalEnum parameters**

  **Dependencies:** Increment 53 implemented and merged.

  Keep all upstream-owned SpinalHDL `core`, `lib` and `idslplugin`
  production sources byte-identical. In MorphHDL-owned post-publication
  code, discover exact `SpinalEnum` definitions, elements and encodings from
  the native graph, replace global enum `` `define `` references and long
  enum-prefixed constants with module-local Verilog-2001 `localparam`s named
  only by the element (`IDLE`, `RUN`, and so on). Retain encoding-specific
  values and one-hot index helpers, remove recognized global macros from the
  final `MorphVerilog` output, and allow the same short names in different
  module scopes. Fail closed on conflicting same-module names or existing
  identifiers rather than adding module or enum prefixes. Ordinary
  `SpinalVerilog` output must remain unchanged. Prove deterministic
  dual-Scala generation, strict Verilog-2001 lint/synthesis and the native
  source-preservation boundary.
'''
          new_todo = '''- [x] **Increment 53b — MorphHDL-owned module-local SpinalEnum parameters**

  **Dependencies:** Increment 53 implemented and merged.

  Keep all upstream-owned SpinalHDL `core`, `lib` and `idslplugin`
  production sources byte-identical. In MorphHDL-owned post-publication
  code, discover exact `SpinalEnum` definitions, elements and encodings from
  the native graph, replace global enum `` `define `` references with
  module-local Verilog-2001 `localparam`s named by the uppercase enum and
  element, for example Scala `State.IDLE` becomes Verilog `STATE_IDLE`.
  Never add a component, module or hierarchy prefix. Retain encoding-specific
  values and one-hot index helpers, remove recognized global macros from the
  final `MorphVerilog` output, and allow identical names in different module
  scopes. Fail closed on conflicting final names or existing identifiers.
  Ordinary `SpinalVerilog` output must remain unchanged. In both supported
  Scala lanes, formally prove the native macro RTL and MorphHDL localparam RTL
  equivalent at the concrete parameter witness using Yosys `equiv_make`,
  sequential induction and `equiv_status -assert`, in addition to deterministic
  generation, strict Verilog-2001 lint/synthesis and native-source preservation.
'''
          if old_todo not in todo:
              raise SystemExit("Increment 53b roadmap anchor is missing")
          todo_path.write_text(todo.replace(old_todo, new_todo, 1), encoding="utf-8")
          PY

          git config user.name "MorphHDL Increment Agent"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            morphhdl/src/main/scala/spinal/core/internals/MorphHdlExternalEnumLocalizer.scala \
            morphhdl/src/test/scala/morphhdl/SpinalEnumLocalParameterTests.scala \
            .github/workflows/morphhdl-enum-localparams.yml \
            docs/morphhdl/parameterized-verilog-todo.md
          if git diff --cached --quiet; then
            echo "No repair changes were required"
            exit 0
          fi
          git commit -m "fix(morphhdl): uppercase enum localparams and prove macro equivalence [inc53b-uppercase-patched]"
          git push origin "HEAD:${GITHUB_REF_NAME}"
