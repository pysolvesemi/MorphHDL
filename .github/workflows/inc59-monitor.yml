name: Increment 59 completion monitor

on:
  push:
    branches:
      - agent/inc59-monitor

permissions:
  contents: read

concurrency:
  group: increment-59-completion-monitor
  cancel-in-progress: true

jobs:
  retire:
    runs-on: ubuntu-latest
    steps:
      - name: Retire obsolete Increment 59 monitor
        run: echo 'The authoritative completion path is PR #143.'
