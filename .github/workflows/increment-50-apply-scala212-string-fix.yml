name: Apply Increment 50 Scala 2.12 String compatibility fix

on:
  push:
    branches:
      - agent/increment-50-native-int-expressions
    paths:
      - .github/increment-50-scala212-string-fix.patch
      - .github/workflows/increment-50-apply-scala212-string-fix.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: increment-50-apply-scala212-string-fix
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/increment-50-native-int-expressions
          fetch-depth: 0

      - name: Apply reviewed compatibility fix and remove delivery scaffolding
        shell: bash
        run: |
          set -euo pipefail
          git apply --check .github/increment-50-scala212-string-fix.patch
          git apply .github/increment-50-scala212-string-fix.patch
          rm -f \
            .github/increment-50-scala212-string-fix.patch \
            .github/workflows/increment-50-apply-scala212-string-fix.yml

          git config user.name "pysolvesemi"
          git config user.email "pysolvesemi@gmail.com"
          git add -A
          git diff --cached --check
          git commit -m "Keep Increment 50 compiler compatible with Scala 2.12"
          git push origin HEAD:agent/increment-50-native-int-expressions
