name: MorphHDL Increment 53c CI dispatch cleanup

on:
  push:
    branches:
      - agent/increment-53c-reconcile-20260828
  workflow_dispatch:

concurrency:
  group: morphhdl-increment-53c-ci-dispatch-watchdog
  cancel-in-progress: true

jobs:
  cleanup:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Confirm dispatch watchdog is superseded by the merged Increment 53c
        run: echo "Increment 53c is merged; no proof-dispatch polling remains."
